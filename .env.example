# Copy this file to .env and fill in real values locally.
# .env is git-ignored - never commit it.
# AWS credentials themselves should NOT go here - use `aws configure`
# (stored in ~/.aws/credentials, outside the repo) or a named profile instead.

# Example of a non-AWS local secret this project might need later:
# COGNITO_TEST_USER_PASSWORD=
